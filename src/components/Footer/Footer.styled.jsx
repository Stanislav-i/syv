import styled from 'styled-components';

export const FooterEl = styled.header`
  box-shadow: var(--shadow);
  background-color: var(--colors-ui-base);
  height: 75px;
  display: flex;
  align-items: center;
`;

export const FooterText = styled.div`
    text-align: center;
    font-size: 12px;
`